// Maximum number of output lines to keep in init script state (prevents unbounded memory growth)
export const MAX_INIT_OUTPUT_LINES = 500;

// Maximum number of file history entries to keep
export const MAX_FILE_HISTORY_ENTRIES = 50;

// Default auto-save interval in milliseconds (30 seconds)
export const DEFAULT_AUTO_SAVE_INTERVAL_MS = 30_000;

// Editor preference defaults
export const DEFAULT_EDITOR_FONT_SIZE = 13;
export const DEFAULT_EDITOR_TAB_SIZE = 2;
export const DEFAULT_EDITOR_LINE_HEIGHT = 1.5;
